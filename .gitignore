# sbt / Scala
target/
project/target/
project/project/
.bsp/
.metals/
.bloop/

# HRF Arcs art assets mirrored locally for the renderer (large; fetch separately).
# Populate assets/webp2/arcs/images/ by extracting an hrfn-arcs-assets zip (the
# art's category layout `hrfn/arcs/assets/<X>` maps 1:1 to `webp2/arcs/images/<X>`).
/assets/
hrfn-arcs-assets-*.zip

# Generated board-render outputs (RenderExport / RenderSmoke).
/hrf-web/render-out/

# Secrets
.env
*.token
application.conf.local

# Bot runtime artifacts (SQLite game store + launch logs)
*.db
bot.log
bot.err.log
bot.crash-*.log
tunnel.log
tunnel.out.log

# OS / editor
.DS_Store
.idea/
*.iml

# self-play scratch (error dumps from arcs.BaseHost path)
/arcs/
target/
